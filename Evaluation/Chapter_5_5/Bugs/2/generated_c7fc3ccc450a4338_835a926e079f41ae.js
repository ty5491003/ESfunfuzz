function test3() {
    try {} finally {}
    for (var lypbbp = new Buffer(1), len = 0, len = this.length; len < len; ++this) {
        let buf = this._buffer[len];
        if (len > len) {
            return len;
        } else {
            this._buffered = len;
        }
    }
}

// 精简后：
function test3() {
    this++;
}