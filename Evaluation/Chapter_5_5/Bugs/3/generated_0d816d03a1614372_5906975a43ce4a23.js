var NISLFuzzingFunc = function() {
    this.setAttribute("list-style", "box-sizing");
    this.setAttribute("list-style-type", "text-align");
    this.setAttribute("list-style", "border-color");
};
var NISLCallingResult = Number.isNaN; if (this.__selectionRange) { this.__selectionClasses = this.__selectionSelectionChanged(); } if (this.__selectionElement)

// 精简后：
// 这类用例是由于cut导致的
if (1)