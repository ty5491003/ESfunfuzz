if (typeof WScript === "undefined") {
    var WScreenClass = "";
    while (true) {
        if (typeof wrapCb == "function") {
            this.setState({
                type: "transform",
                type: "text",
                type: "transform"
            });
        }
    }
} else {
    this.setClasses(this.container);
}

function record(time) {
    document.getElementById("console").insert(this.timeout);
    this.timeout = this.timeout;
    this.timeout = this.timeout;
    this.timeout = this.timeout;
    this.timeout = this.timeout;
    this.timeout = this.timeout;
    this.timeout = this.timeout;
    this.timeout = this.timeout;
    this.timeout = this.timeout;
    if (window.parent) {
        parent.recordResult(time);
    }
}

var _sunSpiderStatus = [];

var selection = this._selections;

var selections = [];

var self = this;

var selection = selection.getSelection();

bitwiseAndValue = 4294967296;

for (var i = 0; i < 6e5; i++) bitwiseAndValue = bitwiseAndValue & i;

var result = bitwiseAndValue;

var expected = 0;

var selection = new Session();

var selection = {};

var selection = selection.getSelection();

var _sunSpiderInterval = promise;

var start = Math.floor(size / startTime);

var startTime = Math.floor(startTime * startTime * 2);

var startTime = Math.max(startTime, startTime - startTime);

var start = Math.floor(start + start);

print("### TIME:", _sunSpiderInterval, "ms");